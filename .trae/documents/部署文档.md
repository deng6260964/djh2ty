# 英语家教教学管理系统 - 部署文档

## 1. 部署概述

本文档详细介绍了如何在2c2g服务器上部署英语家教教学管理系统。系统采用前后端分离架构，前端使用Vue.js，后端使用Flask，数据库使用SQLite。

**服务器要求：**

* CPU: 2核心

* 内存: 2GB RAM

* 存储: 20GB+ 可用空间

* 操作系统: Ubuntu 20.04 LTS 或 CentOS 8+

* 网络: 公网IP，开放80、443端口

**部署架构：**

```
用户 → Nginx → Vue.js前端 → Flask后端 → SQLite数据库
                    ↓
                Redis缓存
```

## 2. 服务器环境准备

### 2.1 系统更新

```bash
# Ubuntu系统
sudo apt update && sudo apt upgrade -y

# CentOS系统
sudo yum update -y
```

### 2.2 安装基础软件

```bash
# Ubuntu系统
sudo apt install -y curl wget git vim nginx supervisor

# CentOS系统
sudo yum install -y curl wget git vim nginx supervisor
```

### 2.3 安装Node.js

```bash
# 安装Node.js 18.x
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version
```

### 2.4 安装Python环境

```bash
# Ubuntu系统
sudo apt install -y python3 python3-pip python3-venv

# CentOS系统
sudo yum install -y python3 python3-pip

# 验证安装
python3 --version
pip3 --version
```

### 2.5 安装Redis

```bash
# Ubuntu系统
sudo apt install -y redis-server

# CentOS系统
sudo yum install -y redis

# 启动Redis服务
sudo systemctl start redis
sudo systemctl enable redis

# 验证Redis
redis-cli ping
```

## 3. 项目部署

### 3.1 创建部署目录

```bash
# 创建应用目录
sudo mkdir -p /var/www/teaching-system
sudo chown $USER:$USER /var/www/teaching-system
cd /var/www/teaching-system

# 创建子目录
mkdir -p frontend backend logs uploads
```

### 3.2 部署后端服务

#### 3.2.1 创建Python虚拟环境

```bash
cd /var/www/teaching-system/backend
python3 -m venv venv
source venv/bin/activate
```

#### 3.2.2 安装Python依赖

创建 `requirements.txt` 文件：

```txt
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-JWT-Extended==4.5.3
Flask-CORS==4.0.0
Flask-Migrate==4.0.5
Werkzeug==2.3.7
redis==5.0.1
gunicorn==21.2.0
Pillow==10.0.1
python-dotenv==1.0.0
```

安装依赖：

```bash
pip install -r requirements.txt
```

#### 3.2.3 创建Flask应用

创建 `app.py` 主文件：

```python
from flask import Flask, request, jsonify
from flask_sqlalchemy import SQLAlchemy
from flask_jwt_extended import JWTManager
from flask_cors import CORS
import os
from datetime import timedelta

app = Flask(__name__)

# 配置
app.config['SECRET_KEY'] = os.environ.get('SECRET_KEY', 'your-secret-key-here')
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///teaching_system.db'
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False
app.config['JWT_SECRET_KEY'] = os.environ.get('JWT_SECRET_KEY', 'jwt-secret-string')
app.config['JWT_ACCESS_TOKEN_EXPIRES'] = timedelta(hours=24)
app.config['UPLOAD_FOLDER'] = '/var/www/teaching-system/uploads'
app.config['MAX_CONTENT_LENGTH'] = 16 * 1024 * 1024  # 16MB max file size

# 初始化扩展
db = SQLAlchemy(app)
jwt = JWTManager(app)
CORS(app)

# 导入路由
from routes import auth, courses, homework, exams, students

app.register_blueprint(auth.bp)
app.register_blueprint(courses.bp)
app.register_blueprint(homework.bp)
app.register_blueprint(exams.bp)
app.register_blueprint(students.bp)

if __name__ == '__main__':
    with app.app_context():
        db.create_all()
    app.run(debug=False, host='0.0.0.0', port=5000)
```

#### 3.2.4 创建环境配置文件

创建 `.env` 文件：

```bash
SECRET_KEY=your-very-secure-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here
DATABASE_URL=sqlite:///teaching_system.db
REDIS_URL=redis://localhost:6379/0
FLASK_ENV=production
UPLOAD_FOLDER=/var/www/teaching-system/uploads
```

#### 3.2.5 初始化数据库

```bash
# 激活虚拟环境
source venv/bin/activate

# 初始化数据库
python -c "from app import app, db; app.app_context().push(); db.create_all()"

# 创建初始管理员账户
python init_db.py
```

### 3.3 部署前端应用

#### 3.3.1 构建前端项目

```bash
cd /var/www/teaching-system/frontend

# 如果有源码，先拉取代码
# git clone <your-frontend-repo> .

# 安装依赖
npm install

# 构建生产版本
npm run build
```

#### 3.3.2 配置前端环境变量

创建 `.env.production` 文件：

```bash
VUE_APP_API_BASE_URL=https://yourdomain.com/api
VUE_APP_UPLOAD_URL=https://yourdomain.com/uploads
```

## 4. Nginx配置

### 4.1 创建Nginx配置文件

创建 `/etc/nginx/sites-available/teaching-system`：

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    
    # 重定向到HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com www.yourdomain.com;
    
    # SSL配置
    ssl_certificate /etc/ssl/certs/yourdomain.com.crt;
    ssl_certificate_key /etc/ssl/private/yourdomain.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    
    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
    
    # 前端静态文件
    location / {
        root /var/www/teaching-system/frontend/dist;
        try_files $uri $uri/ /index.html;
        
        # 缓存静态资源
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # API代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # 文件上传
    location /uploads/ {
        alias /var/www/teaching-system/uploads/;
        
        # 安全设置
        location ~* \.(php|jsp|cgi|asp|aspx)$ {
            deny all;
        }
    }
    
    # 日志配置
    access_log /var/log/nginx/teaching-system.access.log;
    error_log /var/log/nginx/teaching-system.error.log;
    
    # 文件上传大小限制
    client_max_body_size 20M;
}
```

### 4.2 启用站点配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/teaching-system /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
sudo systemctl enable nginx
```

## 5. SSL证书配置

### 5.1 使用Let's Encrypt免费证书

```bash
# 安装Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# 设置自动续期
sudo crontab -e
# 添加以下行：
# 0 12 * * * /usr/bin/certbot renew --quiet
```

### 5.2 手动配置SSL证书

如果使用购买的SSL证书：

```bash
# 创建证书目录
sudo mkdir -p /etc/ssl/certs /etc/ssl/private

# 复制证书文件
sudo cp yourdomain.com.crt /etc/ssl/certs/
sudo cp yourdomain.com.key /etc/ssl/private/

# 设置权限
sudo chmod 644 /etc/ssl/certs/yourdomain.com.crt
sudo chmod 600 /etc/ssl/private/yourdomain.com.key
```

## 6. 进程管理

### 6.1 使用Supervisor管理Flask应用

创建 `/etc/supervisor/conf.d/teaching-system.conf`：

```ini
[program:teaching-system]
command=/var/www/teaching-system/backend/venv/bin/gunicorn --workers 2 --bind 127.0.0.1:5000 app:app
directory=/var/www/teaching-system/backend
user=www-data
autostart=true
autorestart=true
stdout_logfile=/var/www/teaching-system/logs/gunicorn.log
stderr_logfile=/var/www/teaching-system/logs/gunicorn.error.log
environment=PATH="/var/www/teaching-system/backend/venv/bin"
```

启动服务：

```bash
# 重新加载配置
sudo supervisorctl reread
sudo supervisorctl update

# 启动应用
sudo supervisorctl start teaching-system

# 查看状态
sudo supervisorctl status
```

### 6.2 使用systemd管理服务

创建 `/etc/systemd/system/teaching-system.service`：

```ini
[Unit]
Description=Teaching System Flask App
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/var/www/teaching-system/backend
Environment=PATH=/var/www/teaching-system/backend/venv/bin
ExecStart=/var/www/teaching-system/backend/venv/bin/gunicorn --workers 2 --bind 127.0.0.1:5000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl start teaching-system
sudo systemctl enable teaching-system
sudo systemctl status teaching-system
```

## 7. 数据库备份策略

### 7.1 创建备份脚本

创建 `/var/www/teaching-system/scripts/backup.sh`：

```bash
#!/bin/bash

# 配置
DB_PATH="/var/www/teaching-system/backend/teaching_system.db"
BACKUP_DIR="/var/www/teaching-system/backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/teaching_system_$DATE.db"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
sqlite3 $DB_PATH ".backup $BACKUP_FILE"

# 压缩备份文件
gzip $BACKUP_FILE

# 删除7天前的备份
find $BACKUP_DIR -name "*.gz" -mtime +7 -delete

echo "Backup completed: ${BACKUP_FILE}.gz"
```

设置权限和定时任务：

```bash
# 设置执行权限
chmod +x /var/www/teaching-system/scripts/backup.sh

# 添加到crontab（每天凌晨2点备份）
sudo crontab -e
# 添加：
# 0 2 * * * /var/www/teaching-system/scripts/backup.sh
```

### 7.2 数据恢复

```bash
# 停止应用
sudo systemctl stop teaching-system

# 恢复数据库
gunzip -c /var/www/teaching-system/backups/teaching_system_20240118_020000.db.gz > /tmp/restore.db
sqlite3 /var/www/teaching-system/backend/teaching_system.db ".restore /tmp/restore.db"

# 重启应用
sudo systemctl start teaching-system
```

## 8. 监控和日志

### 8.1 日志配置

创建日志轮转配置 `/etc/logrotate.d/teaching-system`：

```
/var/www/teaching-system/logs/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 644 www-data www-data
    postrotate
        systemctl reload teaching-system
    endscript
}
```

### 8.2 系统监控脚本

创建 `/var/www/teaching-system/scripts/monitor.sh`：

```bash
#!/bin/bash

# 检查服务状态
check_service() {
    if ! systemctl is-active --quiet $1; then
        echo "$(date): Service $1 is down, restarting..." >> /var/log/teaching-system-monitor.log
        systemctl restart $1
    fi
}

# 检查磁盘空间
check_disk_space() {
    USAGE=$(df /var/www/teaching-system | awk 'NR==2 {print $5}' | sed 's/%//')
    if [ $USAGE -gt 80 ]; then
        echo "$(date): Disk usage is ${USAGE}%" >> /var/log/teaching-system-monitor.log
    fi
}

# 检查内存使用
check_memory() {
    MEMORY_USAGE=$(free | awk 'NR==2{printf "%.2f", $3*100/$2 }')
    if (( $(echo "$MEMORY_USAGE > 90" | bc -l) )); then
        echo "$(date): Memory usage is ${MEMORY_USAGE}%" >> /var/log/teaching-system-monitor.log
    fi
}

# 执行检查
check_service teaching-system
check_service nginx
check_service redis
check_disk_space
check_memory
```

设置定时监控：

```bash
# 每5分钟检查一次
*/5 * * * * /var/www/teaching-system/scripts/monitor.sh
```

## 9. 性能优化

### 9.1 Nginx优化

编辑 `/etc/nginx/nginx.conf`：

```nginx
user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    # 基础设置
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    
    # 压缩设置
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;
    
    # 缓存设置
    open_file_cache max=1000 inactive=20s;
    open_file_cache_valid 30s;
    open_file_cache_min_uses 2;
    open_file_cache_errors on;
}
```

### 9.2 Redis优化

编辑 `/etc/redis/redis.conf`：

```conf
# 内存优化（2GB服务器建议设置为512MB）
maxmemory 512mb
maxmemory-policy allkeys-lru

# 持久化设置
save 900 1
save 300 10
save 60 10000

# 网络优化
tcp-keepalive 300
timeout 0
```

### 9.3 SQLite优化

在Flask应用中添加SQLite优化：

```python
# 在app.py中添加
@app.before_first_request
def optimize_sqlite():
    db.engine.execute('PRAGMA journal_mode=WAL')
    db.engine.execute('PRAGMA synchronous=NORMAL')
    db.engine.execute('PRAGMA cache_size=10000')
    db.engine.execute('PRAGMA temp_store=memory')
```

## 10. 安全配置

### 10.1 防火墙设置

```bash
# 安装ufw
sudo apt install -y ufw

# 默认策略
sudo ufw default deny incoming
sudo ufw default allow outgoing

# 允许SSH
sudo ufw allow ssh

# 允许HTTP和HTTPS
sudo ufw allow 80
sudo ufw allow 443

# 启用防火墙
sudo ufw enable

# 查看状态
sudo ufw status
```

### 10.2 Fail2ban配置

```bash
# 安装fail2ban
sudo apt install -y fail2ban

# 创建配置文件
sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local

# 编辑配置
sudo vim /etc/fail2ban/jail.local
```

添加Nginx保护：

```ini
[nginx-http-auth]
enabled = true
filter = nginx-http-auth
logpath = /var/log/nginx/teaching-system.error.log
maxretry = 3
bantime = 3600

[nginx-limit-req]
enabled = true
filter = nginx-limit-req
logpath = /var/log/nginx/teaching-system.error.log
maxretry = 10
bantime = 600
```

### 10.3 定期安全更新

创建自动更新脚本 `/var/www/teaching-system/scripts/security-update.sh`：

```bash
#!/bin/bash

# 更新系统包
sudo apt update
sudo apt upgrade -y

# 更新Python包
source /var/www/teaching-system/backend/venv/bin/activate
pip list --outdated --format=freeze | grep -v '^\-e' | cut -d = -f 1 | xargs -n1 pip install -U

# 重启服务
sudo systemctl restart teaching-system
sudo systemctl restart nginx

echo "$(date): Security update completed" >> /var/log/security-update.log
```

## 11. 故障排除

### 11.1 常见问题诊断

**服务无法启动：**

```bash
# 检查服务状态
sudo systemctl status teaching-system

# 查看日志
sudo journalctl -u teaching-system -f

# 检查端口占用
sudo netstat -tlnp | grep :5000
```

**数据库连接问题：**

```bash
# 检查数据库文件权限
ls -la /var/www/teaching-system/backend/teaching_system.db

# 测试数据库连接
sqlite3 /var/www/teaching-system/backend/teaching_system.db ".tables"
```

**Nginx配置问题：**

```bash
# 测试配置
sudo nginx -t

# 查看错误日志
sudo tail -f /var/log/nginx/error.log

# 检查端口监听
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :443
```

### 11.2 性能问题排查

**内存使用监控：**

```bash
# 查看内存使用
free -h

# 查看进程内存使用
top -o %MEM

# 查看具体进程
ps aux | grep gunicorn
```

**磁盘空间监控：**

```bash
# 查看磁盘使用
df -h

# 查找大文件
sudo find /var/www/teaching-system -type f -size +100M

# 清理日志
sudo journalctl --vacuum-time=7d
```

### 11.3 应急恢复流程

**服务完全宕机：**

1. 检查系统资源：`htop`, `df -h`, `free -h`
2. 重启核心服务：

   ```bash
   sudo systemctl restart redis
   sudo systemctl restart teaching-system
   sudo systemctl restart nginx
   ```
3. 如果数据库损坏，从备份恢复
4. 检查日志找出根本原因

**数据丢失恢复：**

1. 立即停止所有服务
2. 从最近的备份恢复数据库
3. 检查数据完整性
4. 重启服务并验证功能

## 12. 维护计划

### 12.1 日常维护（每日）

* 检查服务状态

* 监控系统资源使用

* 查看错误日志

* 验证备份完成

### 12.2 周期维护（每周）

* 更新系统安全补丁

* 清理临时文件和日志

* 检查SSL证书有效期

* 性能指标分析

### 12.3 定期维护（每月）

* 完整系统备份

* 数据库优化和清理

* 安全审计

* 容量规划评估

## 13. 扩展建议

### 13.1 水平扩展

当单台服务器无法满足需求时：

1. **负载均衡**：使用Nginx upstream配置多个后端服务器
2. **数据库分离**：迁移到PostgreSQL或MySQL
3. **缓存集群**：Redis Cluster或Redis Sentinel
4. **CDN加速**：静态资源使用CDN分发

### 13.2 监控升级

1. **APM工具**：集成New Relic或Datadog
2. **日志聚合**：使用ELK Stack
3. **告警系统**：配置PagerDuty或钉钉告警
4. **性能监控**：Grafana + Prometheus

### 13.3 安全加固

1. **WAF防护**：Cloudflare或AWS WAF
2. **DDoS防护**：云服务商DDoS防护
3. **入侵检测**：OSSEC或Wazuh
4. **漏洞扫描**：定期安全扫描

***

**部署完成后验证清单：**

* [ ] 前端页面正常访问

* [ ] API接口正常响应

* [ ] 用户注册登录功能正常

* [ ] 文件上传功能正常

* [ ] 数据库读写正常

* [ ] SSL证书配置正确

* [ ] 备份脚本运行正常

* [ ] 监控脚本运行正常

* [ ] 防火墙规则配置正确

* [ ] 服务自启动配置正确

**技术支持联系方式：**

* 系统管理员：\[管理员邮箱]

* 紧急联系电话：\[紧急电话]

* 技术文档：\[文档地址]

